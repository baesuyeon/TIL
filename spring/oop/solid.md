# 객체지향 프로그래밍

## 객체 지향 설계의 5원칙 - SOLID
* SRP: 단일 책임 원칙(single responsibility principle)
* OCP: 개방-폐쇄 원칙 (Open/closed principle)
* LSP: 리스코프 치환 원칙 (Liskov substitution principle)
* ISP: 인터페이스 분리 원칙 (Interface segregation principle)
* DIP: 의존관계 역전 원칙 (Dependency inversion principle)

### SRP(단일 책임 원칙)
* 한 클래스는 **하나의 책임/역할**만 가져야 한다.
* 중요한 기준은 변경이다. 변경이 발생했을 때 영향 범위가 좁다면 단일 책임 원칙을 잘 따른 것이다.

왜 SPR가 중요할까?
* 코드의 재사용이 쉬워진다.
  * 단일 책임 원칙을 지키는 코드는 각각의 코드가 응집력이 높다.
  * 캡슐화를 통해 한 클래스의 변경이 다른 클래스에 영향을 미치지 않도록 만든다.
* 변경이 필요할 때 수정 대상이 명확하다 (요구사항 수정시 변경 범위가 좁다)

=> 즉 유지보수성이 좋아진다.

### OCP(개방 폐쇄 원칙)
확장에는 열려 있고, 수정에는 닫혀 있어야 한다.
즉 새 기능을 추가할 때 기존 코드를 건드리지 않고 확장할 수 있어야 한다.

새 기능을 확장하려면 보통 인터페이스를 구현한 새로운 클래스를 추가하면 된다.(다형성)
하지만 문제는 구현 객체를 바꿔쓰려면 클라이언트 코드도 함께 수정해야한다는 점이다.
이러면 결국 기존 코드가 변경되므로 OCP 원칙을 지킬 수 없게된다.

이 문제를 해결하기 위해 **객체를 생성하고 연관관계를 맺어주는** 별도의 조립자(설정자)가 필요하다.
조립자가 구현 객체를 교체해주면 클라이언트 코드는 변경없이 그대로 사용할 수 있고 OCP를 자연스럽게 지킬 수 있게된다.

### LSP(리스코프 치환 원칙)
자식 클래스는 상위 클래스를 완전히 대체할 수 있어야 한다.
= 부모 클래스가 할 수 있는 행동은 자식 클래스도 할 수 있어야 한다.

왜 LSP가 중요할까?
* 우리가 다형성을 믿고 사용하는 이유는 상위 인터페이스만 바라보고 코드를 작성해도 하위 클래스가 규약을 지켜서 정상 작동할 거라는 기대 때문이다.
* 이를 위해서는 하위 타입은 “인터페이스 규약”을 반드시 따라야 한다.

단순히 컴파일에 성공하는 것을 넘어서는 이야기이다. 행동(Behavior)까지 부모 타입과 호환되어야 한다.

🚗 직관적인 예시
```text
interface Car {
    fun accelerate()
}
```

Car.accelerate()는 ‘앞으로 움직여라’라는 규약이라고 하자.
그런데 어떤 하위 클래스가 accelerate()를 '뒤로 이동'으로 구현해버리면?

타입에도 문제가 없고 컴파일도 되지만 행동 규약이 깨진다. → LSP 위반
“느리더라도 앞으로 가야 한다”
행동 규약을 지키는 것이 핵심.

### ISP(인터페이스 분리 원칙)
클라이언트는 자신이 사용하지 않는 메서드에 의존관계를 맺으면 안된다.
한 인터페이스에 너무 많은 기능을 몰아넣지 말고 필요한 기능끼리 잘게 나눠야 한다.

왜 ISP가 중요할까?
* 불필요한 구현을 강요하지 않는다
  * 인터페이스가 너무 크면 구현 클래스가 “쓰지도 않는 메서드”를 억지로 구현해야 한다.
* 사용자(클라이언트)에게 불필요한 기능을 드러내지 않는다
* API가 불필요하게 복잡해진다.

### DIP(의존관계 역전 법칙)
구체적인 것에 의존하지 말고 추상화(인터페이스)에 의존하라.
변하기 쉬운 구현체 대신 변하지 않는 인터페이스(역할)에 의존해야 한다는 뜻이다.

왜 DIP가 중요할까?
* 구현체는 언제든 바뀔 수 있다 구현체에 의존하게 되면 변경이 아주 어려워진다
  * 할인 정책의 변경
  * 저장소의 변경 (DB → Redis)
  * 알림 방식의 변경 (SMS → Push)

🔑 객체지향의 핵심은 다형성이다.
* 하지만 다형성만으로는 쉽게 부품을 갈아 끼우듯이 개발할 수 없다.
  * 구현 객체를 변경할 때 클라이언트 코드도 함께 변경되기 때문에 다형성 만으로는 OCP, DIP를 지킬 수 없다.
* 스프링은 **DI** 기술을 지원해 OCP, DIP를 지킬 수 있도록 한다.

🔑 DI(Dependency Injection): 의존성 주입
구현체를 외부에서 주입받게 해서 코드 변경 없이 기능 교체 가능
DI 컨테이너 제공하여 객체 생성·관리·주입을 자동으로 처리해준다.

🧑‍💻 실무에서는
이론적으로는 모든 클래스 앞에 인터페이스를 두는 게 이상적이지만
인터페이스를 만들면 항상 추상화 비용이 든다. (파일 증가, 구조 복잡도 증가, 설계 오버헤드)
기능 확장 가능성이 전혀 없는 단순 기능까지 인터페이스를 만들 필요는 없다.

당장 확장 가능성이 없다면 그냥 구체 클래스를 사용한다.
하지만 나중에 역할이 늘어나거나 변경 가능성이 생기면
그때 리팩터링해서 인터페이스를 도입하면 된다.

즉 확장성이 필요한 지점만 전략적으로 추상화하는 것이 실무적인 접근이다.